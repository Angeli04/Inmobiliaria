@using Inmobiliaria.Models
@model IEnumerable<Inmobiliaria.Models.Inquilino>
@{
    ViewData["Title"] = "Inquilinos";
}

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Inquilinos</h5>
        <a asp-action="Create" class="btn btn-primary">Nuevo inquilino</a>
    </div>
    <div class="card-body">
        @await Html.PartialAsync("_Alerts")

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>DNI</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Teléfono</th>
                    <th>Email</th>
                    <th style="width: 170px;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Dni</td>
                            <td>@item.Nombre</td>
                            <td>@item.Apellido</td>
                            <td>@item.Telefono</td>
                            <td>@item.Email</td>
                            <td>
                                <a asp-action="Details" asp-route-id="@item.IdInquilino" class="btn btn-sm btn-outline-secondary">Ver</a>
                                <a asp-action="Edit" asp-route-id="@item.IdInquilino" class="btn btn-sm btn-outline-primary">Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.IdInquilino" class="btn btn-sm btn-outline-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" class="text-center">No hay inquilinos aún.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
